@page
@model SAAS_Projectplanningtool.Pages.ArticleManagement.IndexModel

@{
    ViewData["Title"] = "Artikelverwaltung";
}

<link rel="stylesheet" href="~/css/employee.css" asp-append-version="true" />

<div class="page-header">
    <div class="header-content">
        <div class="header-left">
            <h1><i class="bi bi-box-seam-fill me-2"></i>Artikelverwaltung</h1>
            <p class="header-subtitle">Alle Artikel Ihres Unternehmens</p>
        </div>
        <div class="header-actions">
            <a asp-page="ArticleCategoryIndex" class="btn btn-outline-light">
                <i class="bi bi-tags me-1"></i>Kategorien
            </a>
        </div>
    </div>
</div>

@await Html.PartialAsync("Partials/_ArticleTable", Model)

<script>
    function filterTable() {
        const q      = document.getElementById('searchInput').value.toLowerCase();
        const cat    = document.getElementById('categoryFilter').value;
        const status = document.getElementById('statusFilter').value;
        const rows   = document.querySelectorAll('#articleTable tbody tr');
        let visible  = 0;
        rows.forEach(r => {
            const show = (!q      || r.dataset.search.includes(q))   &&
                         (!cat    || r.dataset.category === cat)      &&
                         (!status || r.dataset.status   === status);
            r.style.display = show ? '' : 'none';
            if (show) visible++;
        });
        document.getElementById('rowCount').textContent = visible;
    }
    filterTable();
</script>

